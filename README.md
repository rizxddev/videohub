VideoHub GitHub-backed demo

- Set env in Vercel: GITHUB_OWNER, GITHUB_REPO, GITHUB_TOKEN, JWT_SECRET
- Run locally: npm install && npm run dev
- Note: GitHub file size limits apply (we enforce 60MB limit)
